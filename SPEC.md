# WOTA抽签软件 - 项目规格文档

## 1. 项目概述

- **项目名称**: WOTA抽签软件
- **项目类型**: Windows桌面应用程序
- **核心功能**: 随机抽签工具，支持数据导入、自定义动画和多种抽签方式
- **目标用户**: 需要进行随机抽签活动的组织和个人
- **技术栈**: Python 3.12 + PyQt5

## 2. 文件结构

```
抽签软件/
├── main.py              # 主程序入口
├── requirements.txt     # 依赖包
├── SPEC.md             # 项目规格文档
├── CLAUDE.md           # AI使用规范
├── data.json           # 数据存储文件（运行时生成）
└── settings.json       # 设置存储文件（运行时生成）
```

## 3. UI/UX规格

### 3.1 窗口结构

- **主窗口**: 单一窗口设计，包含四个主要页面
- **窗口尺寸**: 1000x700像素，固定大小
- **窗口标题**: WOTA抽签软件

### 3.2 页面布局

#### 待机页面 (StandbyPage)
- 背景动画层（鼠标事件穿透）
- 大号标题"抽签系统"居中显示（琥珀色）
- 副标题显示当前导入数据数量
- 三个导航按钮：开始抽签、数据管理、设置

#### 抽签页面 (DrawingPage)
- 滚动显示区（深紫色背景圆角卡片）
- 结果显示区
- 控制按钮：开始/停止、重新抽签、返回待机

#### 数据管理页面 (DataManagerPage)
- 数据列表显示区
- 导入数据按钮（下拉菜单：TXT/CSV）
- 导出CSV按钮
- 清空数据按钮
- 单条添加输入框
- 返回待机按钮

#### 设置页面 (SettingsPage)
- 动画设置组：速度滑块、效果下拉、颜色选择
- 抽签设置组：模式下拉、人数输入（多人模式可见）、重复抽取开关
- 快捷键设置组：5个快捷键可配置
- 保存/重置/返回按钮

### 3.3 视觉设计

**颜色方案**:
| 用途 | 色值 |
|------|------|
| 主色 | #6366F1 |
| 次色 | #4F46E5 |
| 强调色 | #F59E0B |
| 背景色 | #1E1B4B |
| 文字色 | #FFFFFF |
| 卡片背景 | #312E81 |

**字体**: Microsoft YaHei

**间距**: 页面内边距40px，元素间距20px

### 3.4 动画效果

| 效果 | 描述 |
|------|------|
| 粒子效果 | 50个彩色粒子随机漂浮运动 |
| 渐变效果 | 颜色渐变循环动画 |
| 光晕效果 | 脉冲式光晕透明度变化 |

## 4. 功能规格

### 4.1 数据管理

| 功能 | 说明 |
|------|------|
| 导入TXT | 每行一个名字，自动去重 |
| 导入CSV | 第一列为名字，自动去重 |
| 导出CSV | 全部数据导出 |
| 添加单条 | 输入框添加 |
| 清空数据 | 需二次确认 |

**数据存储**: JSON格式，`data.json`文件

### 4.2 抽签功能

| 模式 | 描述 |
|------|------|
| 单人模式 | 抽取1人 |
| 多人模式 | 抽取N人（可设置数量） |
| 淘汰模式 | 逐一淘汰至最后1人 |

**设置项**:
- 抽出人数：1-100
- 允许重复抽取：是/否

### 4.3 快捷键

| 功能 | 默认快捷键 |
|------|-----------|
| 开始/停止抽签 | Space |
| 重新抽签 | R |
| 返回待机 | Escape |
| 打开数据管理 | D |
| 打开设置 | S |

### 4.4 设置持久化

`settings.json`存储内容：
- 动画速度 (10-200)
- 动画效果 (particle/gradient/glow)
- 主色调颜色值
- 抽签模式
- 抽出人数
- 允许重复抽取
- 快捷键配置

## 5. 已知问题与修复记录

### 5.1 已修复问题

| 日期 | 问题 | 修复方案 |
|------|------|----------|
| 2026/05/15 | 动画切换导致崩溃 | 使用`deleteLater()`正确删除旧widget |
| 2026/05/15 | 按钮无法点击 | 动画widget添加`WA_TransparentForMouseEvents` |
| 2026/05/15 | 模式切换崩溃 | 将QHBoxLayout包装为QWidget使用setVisible() |

### 5.2 待优化项

- [ ] 数据列表支持删除单条
- [ ] 快捷键冲突检测
- [ ] 导入文件预览

## 6. 运行说明

### 6.1 安装依赖

```bash
pip install PyQt5
```

### 6.2 运行程序

```bash
python main.py
```

### 6.3 注意事项

- 程序关闭后自动保存数据和设置
- `settings.json`损坏时使用默认设置重置
- `data.json`损坏时清空数据