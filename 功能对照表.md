# WOTA抽签软件 - 功能对照表

> 本文档记录原 PyQt5 版本的所有功能细节，作为重构 PyQt6 版本的参考文档

---

## 1. 项目结构（PyQt6 重构版）

```
抽签软件/
├── main.py # 主程序入口
├── engine/ # 核心抽签逻辑包
│   ├── __init__.py
│   ├── draw.py              # 抽签引擎
│   └── validator.py         # 验证器
├── data/ # 数据管理包
│   ├── __init__.py
│   └── manager.py           # 数据管理
├── ui/                       # UI组件包
│   ├── __init__.py
│   ├── main_window.py       # 主窗口
│   ├── theme.py             # 主题样式
│   ├── pages/               # 页面
│   │   ├── __init__.py
│   │   ├── base.py          # 页面基类
│   │   ├── start.py         # 启动页
│   │   ├── data.py          # 数据管理页
│   │   ├── settings.py # 设置页
│   │   └── more.py          # 更多页
│   └── widgets/             # 组件
│       ├── __init__.py
│       ├── sidebar.py       # 侧边栏
│       ├── animated_bg.py # 动画背景
│       └── toast.py         # 气泡提示
└── config/                   # 配置
    └── settings.json        # 设置存储
```

---

## 2. 抽签模式

### 2.1 单人模式 (single)
- 随机抽出 **1人**
- 显示姓名，大字号橙色文字

### 2.2 多人模式 (multi)
- 根据 `multi_count` 设置抽出指定人数
- 结果分行显示

### 2.3 淘汰模式 (elimination)
- 逐一淘汰直到剩 **1人**
- 返回完整的淘汰顺序

---

## 3. 动画背景效果

### 3.1 纯色 (solid)
- 单色背景 `#E8F4FC` (浅蓝)

### 3.2 粒子 (particle)
- 50个随机粒子
- 碰到边界反弹
- 可配置粒子颜色

### 3.3 渐变 (gradient)
- 两色渐变背景
- 渐变角度随时间变化

### 3.4 光晕 (glow)
- 单色光晕
- 透明度在 0-1 之间脉动

### 3.5 图片 (image)
- 自定义背景图片
- 支持 png/jpg/jpeg/bmp

---

## 4. 数据管理

### 4.1 导入格式
|格式 | 方法 | 去重 |
|------|------|------|
| TXT | `import_txt()` | 是 |
| CSV | `import_csv()` | 是 |
| Excel | `import_excel()` | 是 |

### 4.2 导出格式
| 格式 | 方法 |
|------|------|
| CSV | `export_csv()` |

### 4.3 数据操作
- `add(name)` - 添加单条
- `remove(name)` - 删除单条
- `clear()` - 清空全部

---

## 5. 快捷键

| 功能 | 默认键 |
|------|--------|
| 开始/停止抽签 | Space |
| 重新抽签 | R |
| 返回待机 | Escape |
| 打开数据管理 | D |
| 打开设置 | S |

---

## 6. 气泡提示

### 6.1 类型
| 类型 | 背景色 | 文字色 |
|------|--------|--------|
| info | #312E81 | #FFFFFF |
| success | #059669 | #FFFFFF |
| warning | #F59E0B | #1E1B4B |
| error | #DC2626 | #FFFFFF |

### 6.2 动画
- 淡入：300ms `QEasingCurve.InOutQuad`
- 淡出：300ms
- 显示时长：3000ms

---

## 7. 验证规则 (Validators)

| 验证项 | 错误码 | 说明 |
|--------|--------|------|
| 数据为空 | DATA_EMPTY | 抽签前检测 |
| 抽出人数无效 | INVALID_MULTI_COUNT | 1-100 |
| 抽出人数超限 | MULTI_COUNT_TOO_LARGE | ≤数据总数 |
| 图片不存在 | IMAGE_NOT_FOUND | 路径检测 |
| 图片格式错误 | INVALID_IMAGE_FORMAT | png/jpg/jpeg/bmp |
| 缺少库 | MISSING_LIBRARY | pandas/xlrd |

---

## 8. 主题颜色

| 用途 | 颜色值 |
|------|--------|
| 主色 | #6366F1 |
| 次要色 | #4F46E5 |
| 强调色 | #F59E0B |
| 深色背景 | #1E1B4B |
| 浅色背景 | #E8F4FC |
| 卡片背景 | #312E81 |
| 危险色 | #DC2626 |
| 成功色 | #059669 |

---

## 9. UI布局

### 9.1 主窗口
- 浅蓝色背景 `#E8F4FC`
- 左侧白色侧边栏 (带阴影)
- 右侧白色内容卡片 (带阴影)

### 9.2 侧边栏
- 导航按钮：启动、数据管理、设置、更多
- 底部版本信息
- 悬停背景：`#F0F4F8`
- 激活背景：`#6366F1` (紫色)

### 9.3 启动页
- 左侧：标题 + 数据统计 + 结果显示 + 控制按钮
- 右侧：控制面板 (背景选择 + 数据集列表)

---

## 10. 设置持久化 (settings.json)

```json
{
  "animation_speed": 50,
  "background_type": "solid",
  "primary_color": "#6366F1",
  "particle_color": "#6366F1",
  "secondary_color": "#4F46E5",
  "accent_color": "#F59E0B",
  "background_color": "#1E1B4B",
  "background_image": "",
  "drawing_mode": "single",
  "multi_count": 3,
  "allow_duplicate": false,
  "shortcuts": {
    "start_draw": "Space",
    "reset_draw": "R",
    "back_standby": "Escape",
    "open_data": "D",
    "open_settings": "S"
  }
}
```

---

## 11. 数据持久化 (data.json)

```json
["张三", "李四", "王五", ...]
```

---

## 12. PyQt5 → PyQt6 迁移要点

| PyQt5 | PyQt6 |
|-------|-------|
| `from PyQt5.QtWidgets import ...` | `from PyQt6.QtWidgets import ...` |
| `Qt.Key_Space` | `Qt.Key.Key_Space` |
| `Qt.WA_TransparentForMouseEvents` | `Qt.WidgetAttribute.WA_TransparentForMouseEvents` |
| `Qt.Horizontal` | `Qt.Orientation.Horizontal` |
| `Qt.AlignCenter` | `Qt.AlignmentFlag.AlignCenter` |
| `pyqtSignal` | `pyqtSignal` (相同) |
| `QTimer.singleShot` | `QTimer.singleShot` (相同) |
| `QPropertyAnimation` | `QPropertyAnimation` (相同) |

---

## 13. 关键实现细节

### 13.1 阴影效果
```python
shadow = QGraphicsDropShadowEffect()
shadow.setBlurRadius(10)
shadow.setColor(QColor(0, 0, 0, 60))
shadow.setOffset(2, 0)
widget.setGraphicsEffect(shadow)
```

### 13.2 动画Widget鼠标穿透
```python
content.setAttribute(Qt.WidgetAttribute.WA_TransparentForMouseEvents)
```

### 13.3 Layout可见性 (需用QWidget包装)
```python
self.multi_count_widget = QWidget()
self.multi_count_layout = QHBoxLayout(self.multi_count_widget)
self.multi_count_widget.setVisible(True/False)
```

### 13.4 动画切换必须用deleteLater()
```python
old_widget.deleteLater()  # 避免内存泄漏
```

---

## 14. 技术栈

- **语言**: Python 3.12
- **框架**: PyQt6
- **数据存储**: JSON文件本地持久化
- **依赖**: PyQt6, pandas, openpyxl, xlrd, pytest
- **打包**: PyInstaller

---

## 15. 开发模式：TDD 1+3 方法

> "1个测试用例驱动3个开发迭代"

每个功能开发流程：
1. **编写测试用例** - 明确输入输出预期
2. **迭代1** - 实现基础功能，通过测试
3. **迭代2** - 完善功能边界和错误处理
4. **迭代3** - 优化性能和用户体验

---

## 16. 每次构建目标

| 次数 | 目标 |
|------|------|
| 第1次 | ✅ 项目初始化 + 目录结构 + requirements.txt |
| 第2次 | 核心抽签逻辑 - engine/draw.py (单人/多人/淘汰) |
| 第3次 | 验证器 - engine/validator.py |
| 第4次 | 数据管理 - data/manager.py (导入/导出/持久化) |
| 第5次 | UI框架 - main_window.py + sidebar.py |
| 第6次 | 启动页 - pages/start.py + 动画背景 |
| 第7次 | 其他页面 - data_page + settings_page + more_page |
| 第8次 | 气泡提示 + 快捷键 + 集成测试 |
| 第9次 | 打包验证 + 收尾 |