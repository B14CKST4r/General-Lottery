# WOTA抽签软件 - 功能对照表

> 本文档记录 WOTA 抽签软件的功能规划与实现状态
> - 当前代码位于 `dist/` 目录，采用 PyQt6 重构
> - ✅ = 已完成 | 🔨 = 开发中 | 📋 = 待实现

---

## 1. 项目结构（PyQt6 重构版）

```
抽签软件/
├── main.py                 # ✅ 主程序入口（骨架）
├── dist/
│   ├── engine/             # ✅ 核心抽签逻辑包
│   │   ├── __init__.py
│   │   ├── draw.py         # ✅ 抽签引擎 (15个测试通过)
│   │   └── validator.py    # 📋 验证器
│   ├── data/               # 📋 数据管理包
│   │   ├── __init__.py
│   │   └── manager.py      # 📋 数据管理
│   ├── ui/                 # 📋 UI组件包
│   │   ├── __init__.py
│   │   ├── main_window.py  # 📋 主窗口
│   │   ├── theme.py        # 📋 主题样式
│   │   ├── pages/          # 📋 页面
│   │   │   ├── __init__.py
│   │   │   ├── base.py     # 📋 页面基类
│   │   │   ├── start.py    # 📋 启动页
│   │   │   ├── data.py     # 📋 数据管理页
│   │   │   ├── settings.py # 📋 设置页
│   │   │   └── more.py     # 📋 更多页
│   │   └── widgets/        # 📋 组件
│   │       ├── __init__.py
│   │       ├── sidebar.py  # 📋 侧边栏
│   │       ├── animated_bg.py # 📋 动画背景
│   │       └── toast.py    # 📋 气泡提示
│   └── config/             # 📋 配置
│       └── settings.json   # 📋 设置存储
```

**当前状态**: 仅 `main.py` 创建了基础窗口骨架，其余模块均为空目录占位。

---

## 2. 抽签模式

| 模式 | 内部值 | 功能描述 | 状态 |
|------|--------|----------|------|
| 单人模式 | `single` | 随机抽出 **1人** | ✅ 已实现 |
| 多人模式 | `multi` | 根据 `multi_count` 设置抽出指定人数 | ✅ 已实现 |
| 淘汰模式 | `elimination` | 逐一淘汰直到剩 **1人** | ✅ 已实现 |

---

## 3. 动画背景效果

| 效果 | 内部值 | 功能描述 | 状态 |
|------|--------|----------|------|
| 纯色 | `solid` | 单色背景 `#E8F4FC` (浅蓝) | 📋 待实现 |
| 粒子 | `particle` | 50个随机粒子，碰到边界反弹 | 📋 待实现 |
| 渐变 | `gradient` | 两色渐变背景，渐变角度随时间变化 | 📋 待实现 |
| 光晕 | `glow` | 单色光晕，透明度在 0-1 之间脉动 | 📋 待实现 |
| 图片 | `image` | 自定义背景图片，支持 png/jpg/jpeg/bmp | 📋 待实现 |

---

## 4. 数据管理

### 4.1 导入格式

| 格式 | 方法 | 去重 | 状态 |
|------|------|------|------|
| TXT | `import_txt()` | 是 | 📋 待实现 |
| CSV | `import_csv()` | 是 | 📋 待实现 |
| Excel | `import_excel()` | 是 | 📋 待实现 |

### 4.2 导出格式

| 格式 | 方法 | 状态 |
|------|------|------|
| CSV | `export_csv()` | 📋 待实现 |

### 4.3 数据操作

| 操作 | 方法 | 状态 |
|------|------|------|
| 添加单条 | `add(name)` | 📋 待实现 |
| 删除单条 | `remove(name)` | 📋 待实现 |
| 清空全部 | `clear()` | 📋 待实现 |

---

## 5. 快捷键

| 功能 | 默认键 | 状态 |
|------|--------|------|
| 开始/停止抽签 | `Space` | 📋 待实现 |
| 重新抽签 | `R` | 📋 待实现 |
| 返回待机 | `Escape` | 📋 待实现 |
| 打开数据管理 | `D` | 📋 待实现 |
| 打开设置 | `S` | 📋 待实现 |

**注意**: 需要实现快捷键冲突检测，防止多个功能使用同一快捷键。

---

## 6. 气泡提示

### 6.1 类型

| 类型 | 背景色 | 文字色 | 状态 |
|------|--------|--------|------|
| info | `#312E81` | `#FFFFFF` | 📋 待实现 |
| success | `#059669` | `#FFFFFF` | 📋 待实现 |
| warning | `#F59E0B` | `#1E1B4B` | 📋 待实现 |
| error | `#DC2626` | `#FFFFFF` | 📋 待实现 |

### 6.2 动画

| 属性 | 值 | 状态 |
|------|------|------|
| 淡入时长 | 300ms `QEasingCurve.InOutQuad` | 📋 待实现 |
| 淡出时长 | 300ms | 📋 待实现 |
| 显示时长 | 3000ms | 📋 待实现 |

---

## 7. 验证规则 (Validators)

| 验证项 | 错误码 | 说明 | 状态 |
|--------|--------|------|------|
| 数据为空 | `DATA_EMPTY` | 抽签前检测 | 📋 待实现 |
| 抽出人数无效 | `INVALID_MULTI_COUNT` | 1-100 | 📋 待实现 |
| 抽出人数超限 | `MULTI_COUNT_TOO_LARGE` | ≤数据总数 | 📋 待实现 |
| 图片不存在 | `IMAGE_NOT_FOUND` | 路径检测 | 📋 待实现 |
| 图片格式错误 | `INVALID_IMAGE_FORMAT` | png/jpg/jpeg/bmp | 📋 待实现 |
| 缺少库 | `MISSING_LIBRARY` | pandas/xlrd | 📋 待实现 |

---

## 8. 主题颜色

| 用途 | 颜色值 | 状态 |
|------|--------|------|
| 主色 | `#6366F1` | ✅ 已定义 |
| 次要色 | `#4F46E5` | ✅ 已定义 |
| 强调色 | `#F59E0B` | ✅ 已定义 |
| 深色背景 | `#1E1B4B` | ✅ 已定义 |
| 浅色背景 | `#E8F4FC` | ✅ 已定义 |
| 卡片背景 | `#312E81` | ✅ 已定义 |
| 危险色 | `#DC2626` | ✅ 已定义 |
| 成功色 | `#059669` | ✅ 已定义 |

---

## 9. UI布局

### 9.1 主窗口

| 属性 | 值 | 状态 |
|------|------|------|
| 背景色 | `#E8F4FC` (浅蓝) | ✅ 已定义 |
| 左侧侧边栏 | 白色，带阴影 | 📋 待实现 |
| 右侧内容卡片 | 白色，带阴影 | 📋 待实现 |
| 窗口尺寸 | 1200x800 | ✅ 已定义 |

### 9.2 侧边栏

| 元素 | 说明 | 状态 |
|------|------|------|
| 导航按钮 | 启动、数据管理、设置、更多 | 📋 待实现 |
| 底部版本信息 | 显示版本号 | 📋 待实现 |
| 悬停背景 | `#F0F4F8` | 📋 待实现 |
| 激活背景 | `#6366F1` (紫色) | 📋 待实现 |

### 9.3 启动页

| 区域 | 内容 | 状态 |
|------|------|------|
| 左侧主区域 | 标题 + 数据统计 + 结果显示 + 控制按钮 | 📋 待实现 |
| 右侧控制面板 | 背景选择 + 数据集列表 | 📋 待实现 |

---

## 10. 设置持久化 (settings.json)

| 键名 | 类型 | 默认值 | 状态 |
|------|------|--------|------|
| `animation_speed` | int | 50 | 📋 待实现 |
| `background_type` | str | `"solid"` | 📋 待实现 |
| `primary_color` | str | `"#6366F1"` | ✅ 已定义 |
| `particle_color` | str | `"#6366F1"` | ✅ 已定义 |
| `secondary_color` | str | `"#4F46E5"` | ✅ 已定义 |
| `accent_color` | str | `"#F59E0B"` | ✅ 已定义 |
| `background_color` | str | `"#1E1B4B"` | ✅ 已定义 |
| `background_image` | str | `""` | 📋 待实现 |
| `drawing_mode` | str | `"single"` | 📋 待实现 |
| `multi_count` | int | 3 | 📋 待实现 |
| `allow_duplicate` | bool | `false` | 📋 待实现 |
| `shortcuts` | dict | 见下方 | 📋 待实现 |

**快捷键默认值**:
```python
{
    "start_draw": "Space",
    "reset_draw": "R",
    "back_standby": "Escape",
    "open_data": "D",
    "open_settings": "S"
}
```

---

## 11. 数据持久化 (data.json)

| 格式 | 示例 | 状态 |
|------|------|------|
| JSON数组 | `["张三", "李四", "王五", ...]` | 📋 待实现 |

---

## 12. PyQt5 → PyQt6 迁移要点

| PyQt5 | PyQt6 | 状态 |
|-------|-------|------|
| `from PyQt5.QtWidgets import ...` | `from PyQt6.QtWidgets import ...` | ✅ 已更新 |
| `Qt.Key_Space` | `Qt.Key.Key_Space` | ✅ 已记录 |
| `Qt.WA_TransparentForMouseEvents` | `Qt.WidgetAttribute.WA_TransparentForMouseEvents` | ✅ 已记录 |
| `Qt.Horizontal` | `Qt.Orientation.Horizontal` | ✅ 已记录 |
| `Qt.AlignCenter` | `Qt.AlignmentFlag.AlignCenter` | ✅ 已记录 |
| `pyqtSignal` | `pyqtSignal` (相同) | ✅ 已记录 |
| `QTimer.singleShot` | `QTimer.singleShot` (相同) | ✅ 已记录 |
| `QPropertyAnimation` | `QPropertyAnimation` (相同) | ✅ 已记录 |

---

## 13. 关键实现细节

### 13.1 阴影效果

```python
shadow = QGraphicsDropShadowEffect()
shadow.setBlurRadius(10)
shadow.setColor(QColor(0, 0, 0, 60))
shadow.setOffset(2, 0)
widget.setGraphicsEffect(shadow)
```
**状态**: ✅ 已记录，待实现

### 13.2 动画Widget鼠标穿透

```python
content.setAttribute(Qt.WidgetAttribute.WA_TransparentForMouseEvents)
```
**状态**: ✅ 已记录，待实现

### 13.3 Layout可见性 (需用QWidget包装)

```python
self.multi_count_widget = QWidget()
self.multi_count_layout = QHBoxLayout(self.multi_count_widget)
self.multi_count_widget.setVisible(True/False)
```
**状态**: ✅ 已记录，待实现

### 13.4 动画切换必须用deleteLater()

```python
old_widget.deleteLater()  # 避免内存泄漏
```
**状态**: ✅ 已记录，待实现

---

## 14. 技术栈

| 项目 | 值 | 状态 |
|------|------|------|
| 语言 | Python 3.12 | ✅ 已确定 |
| 框架 | PyQt6 | ✅ 已升级（原 PyQt5） |
| 数据存储 | JSON文件本地持久化 | ✅ 已规划 |
| 依赖 | PyQt6, pandas, openpyxl, xlrd, pytest | ✅ 已规划 |
| 打包 | PyInstaller | 📋 待验证 |

---

## 15. 开发模式：TDD 1+3 方法

> "1个测试用例驱动3个开发迭代"

每个功能开发流程：
1. **编写测试用例** - 明确输入输出预期
2. **迭代1** - 实现基础功能，通过测试
3. **迭代2** - 完善功能边界和错误处理
4. **迭代3** - 优化性能和用户体验

---

## 16. 开发进度总览

| 阶段 | 内容 | 状态 |
|------|------|------|
| 第1次 | 项目初始化 + 目录结构 + requirements.txt | ✅ 完成 |
| 第2次 | 核心抽签逻辑 - engine/draw.py | ✅ 完成（15个测试通过） |
| 第3次 | 验证器 - engine/validator.py | 📋 待开始 |
| 第4次 | 数据管理 - data/manager.py | 📋 待开始 |
| 第5次 | UI框架 - main_window.py + sidebar.py | 📋 待开始 |
| 第6次 | 启动页 - pages/start.py + 动画背景 | 📋 待开始 |
| 第7次 | 其他页面 - data_page + settings_page + more_page | 📋 待开始 |
| 第8次 | 气泡提示 + 快捷键 + 集成测试 | 📋 待开始 |
| 第9次 | 打包验证 + 收尾 | 📋 待开始 |

---

## 17. 待优化项 (从 SPEC.md)

- [ ] 数据列表支持删除单条
- [ ] 快捷键冲突检测
- [ ] 导入文件预览

---

## 18. 扩展建议 (未来规划)

- [ ] 添加数据库支持
- [ ] 支持抽签结果历史记录
- [ ] 添加音效
- [ ] 支持自定义背景图片
- [ ] 添加抽签结果导出为图片功能
- [ ] 多数据集管理
- [ ] 比赛模板管理